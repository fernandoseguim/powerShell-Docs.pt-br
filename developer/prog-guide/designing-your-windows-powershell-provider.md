---
title: Criando o provedor do Windows PowerShell | Microsoft Docs
ms.custom: ''
ms.date: 09/13/2016
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- providers [PowerShell Programmer's Guide], designing
ms.assetid: 11d20319-cc40-4227-b810-4af33372b182
caps.latest.revision: 10
ms.openlocfilehash: 711a85e9b2eade7b9095d7560f53610e709e380a
ms.sourcegitcommit: 69abc5ad16e5dd29ddfb1853e266a4bfd1d59d59
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/05/2019
ms.locfileid: "57429934"
---
# <a name="designing-your-windows-powershell-provider"></a><span data-ttu-id="b50d9-102">Projetar seu provedor do Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="b50d9-102">Designing Your Windows PowerShell Provider</span></span>

<span data-ttu-id="b50d9-103">Se o produto ou a configuração expõe um conjunto de dados armazenados, como um banco de dados que o usuário desejará navegam, você deve implementar um provedor do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-103">You should implement a Windows PowerShell provider if your product or configuration exposes a set of stored data, such as a database that the user will want to navigate or browse.</span></span> <span data-ttu-id="b50d9-104">Além disso, se o seu produto fornece um contêiner, mesmo se ele não é um contêiner de vários níveis, faz sentido implementar um provedor do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-104">Additionally, if your product provides a container, even if it is not a multilevel container, it makes sense to implement a Windows PowerShell provider.</span></span> <span data-ttu-id="b50d9-105">Por exemplo, você talvez queira implementar um provedor de contêiner do Windows PowerShell se cmdlet verbo copiar, mover, renomear, novo ou remover faz sentido como uma operação em seus dados de configuração ou o produto.</span><span class="sxs-lookup"><span data-stu-id="b50d9-105">For example, you might want to implement a Windows PowerShell container provider if the cmdlet verb Copy, Move, Rename, New, or Remove makes sense as an operation on your product or configuration data.</span></span>

## <a name="windows-powershell-paths-identify-your-provider"></a><span data-ttu-id="b50d9-106">Caminhos do Windows PowerShell identificam seu provedor</span><span class="sxs-lookup"><span data-stu-id="b50d9-106">Windows PowerShell Paths Identify Your Provider</span></span>

<span data-ttu-id="b50d9-107">O tempo de execução do Windows PowerShell usa caminhos do Windows PowerShell para acessar o provedor apropriado do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-107">The Windows PowerShell runtime uses Windows PowerShell paths to access the appropriate Windows PowerShell provider.</span></span> <span data-ttu-id="b50d9-108">Quando um cmdlet especifica um desses caminhos, o tempo de execução sabe qual provedor será usado para acessar o armazenamento de dados associado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-108">When a cmdlet specifies one of these paths, the runtime knows which provider to use to access the associated data store.</span></span> <span data-ttu-id="b50d9-109">Esses caminhos incluem caminhos qualificadas pela unidade, caminhos de provedor qualificado, caminhos diretos do provedor e caminhos de provedor interno.</span><span class="sxs-lookup"><span data-stu-id="b50d9-109">These paths include drive-qualified paths, provider-qualified paths, provider-direct paths, and provider-internal paths.</span></span> <span data-ttu-id="b50d9-110">Cada provedor do Windows PowerShell deve oferecer suporte a um ou mais desses caminhos.</span><span class="sxs-lookup"><span data-stu-id="b50d9-110">Each Windows PowerShell provider must support one or more of these paths.</span></span>

<span data-ttu-id="b50d9-111">Para obter mais informações sobre os caminhos do Windows PowerShell, consulte como funciona o Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-111">For more information about Windows PowerShell paths, see How Windows PowerShell Works.</span></span>

### <a name="defining-a-drive-qualified-path"></a><span data-ttu-id="b50d9-112">Definindo um caminho qualificado por unidade</span><span class="sxs-lookup"><span data-stu-id="b50d9-112">Defining a Drive-Qualified Path</span></span>

<span data-ttu-id="b50d9-113">Para permitir que o usuário acesse dados localizados em uma unidade física, o provedor do Windows PowerShell deve dar suporte a um caminho qualificado por unidade.</span><span class="sxs-lookup"><span data-stu-id="b50d9-113">To allow the user to access data located at a physical drive, your Windows PowerShell provider must support a drive-qualified path.</span></span> <span data-ttu-id="b50d9-114">Esse caminho inicia com o nome da unidade seguido por dois-pontos (:), por exemplo, mydrive:\abc\bar.</span><span class="sxs-lookup"><span data-stu-id="b50d9-114">This path starts with the drive name followed by a colon (:), for example, mydrive:\abc\bar.</span></span>

### <a name="defining-a-provider-qualified-path"></a><span data-ttu-id="b50d9-115">Definindo um caminho qualificado provedor</span><span class="sxs-lookup"><span data-stu-id="b50d9-115">Defining a Provider-Qualified Path</span></span>

<span data-ttu-id="b50d9-116">Para permitir que o tempo de execução do Windows PowerShell inicializar e inicializar o provedor, o provedor do Windows PowerShell deve dar suporte a um caminho qualificado pelo provedor.</span><span class="sxs-lookup"><span data-stu-id="b50d9-116">To allow the Windows PowerShell runtime to initialize and uninitialize the provider, your Windows PowerShell provider must support a provider-qualified path.</span></span> <span data-ttu-id="b50d9-117">Por exemplo, sistema de arquivos::\\\uncshare\abc\bar é o caminho de provedor qualificado para o provedor do sistema de arquivos fornecido pelo Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-117">For example, FileSystem::\\\uncshare\abc\bar is the provider-qualified path for the filesystem provider furnished by Windows PowerShell.</span></span>

### <a name="defining-a-provider-direct-path"></a><span data-ttu-id="b50d9-118">Definindo um caminho direto de provedor</span><span class="sxs-lookup"><span data-stu-id="b50d9-118">Defining a Provider-Direct Path</span></span>

<span data-ttu-id="b50d9-119">Para permitir o acesso remoto ao seu provedor do Windows PowerShell, ele deve dar suporte a um caminho direto de provedor de passar diretamente para o provedor do Windows PowerShell para o local atual.</span><span class="sxs-lookup"><span data-stu-id="b50d9-119">To allow remote access to your Windows PowerShell provider, it should support a provider-direct path to pass directly to the Windows PowerShell provider for the current location.</span></span> <span data-ttu-id="b50d9-120">Por exemplo, o provedor de registro do Windows PowerShell pode usar \\\server\regkeypath como um caminho direto de provedor.</span><span class="sxs-lookup"><span data-stu-id="b50d9-120">For example, the registry Windows PowerShell provider can use \\\server\regkeypath as a provider-direct path.</span></span>

### <a name="defining-a-provider-internal-path"></a><span data-ttu-id="b50d9-121">Definindo um caminho de provedor interno</span><span class="sxs-lookup"><span data-stu-id="b50d9-121">Defining a Provider-Internal Path</span></span>

<span data-ttu-id="b50d9-122">Para permitir que o cmdlet do provedor acessar dados usando interfaces de programação de aplicativo (APIs) do não sejam do Windows PowerShell, o provedor do Windows PowerShell deve dar suporte a um caminho de provedor interno.</span><span class="sxs-lookup"><span data-stu-id="b50d9-122">To allow the provider cmdlet to access data using non-Windows PowerShell application programming interfaces (APIs), your Windows PowerShell provider should support a provider-internal path.</span></span> <span data-ttu-id="b50d9-123">Esse caminho é indicado após o "::" no caminho de provedor qualificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-123">This path is indicated after the "::" in the provider-qualified path.</span></span> <span data-ttu-id="b50d9-124">Por exemplo, o caminho de provedor interno para o provedor do sistema de arquivos do Windows PowerShell é \\\uncshare\abc\bar.</span><span class="sxs-lookup"><span data-stu-id="b50d9-124">For example, the provider-internal path for the filesystem Windows PowerShell provider is \\\uncshare\abc\bar.</span></span>

## <a name="changing-stored-data"></a><span data-ttu-id="b50d9-125">Alterando dados armazenados</span><span class="sxs-lookup"><span data-stu-id="b50d9-125">Changing Stored Data</span></span>

<span data-ttu-id="b50d9-126">Ao substituir métodos que modificam o armazenamento de dados subjacente, sempre chamar o [System.Management.Automation.Provider.Cmdletprovider.Writeitemobject\*](/dotnet/api/System.Management.Automation.Provider.CmdletProvider.WriteItemObject) método com a versão mais atualizada do item alterado por que método.</span><span class="sxs-lookup"><span data-stu-id="b50d9-126">When overriding methods that modify the underlying data store, always call the [System.Management.Automation.Provider.Cmdletprovider.Writeitemobject\*](/dotnet/api/System.Management.Automation.Provider.CmdletProvider.WriteItemObject) method with the most up-to-date version of the item changed by that method.</span></span> <span data-ttu-id="b50d9-127">A infraestrutura do provedor determina se o objeto do item precisa ser passado para o pipeline, como quando o usuário Especifica o parâmetro - PassThru.</span><span class="sxs-lookup"><span data-stu-id="b50d9-127">The provider infrastructure determines if the item object needs to be passed to the pipeline, such as when the user specifies the -PassThru parameter.</span></span> <span data-ttu-id="b50d9-128">Se recuperar o item mais atualizado é uma operação cara (em termos de desempenho), você pode testar a propriedade Context.PassThru para determinar se você realmente precisa escrever o item resultante.</span><span class="sxs-lookup"><span data-stu-id="b50d9-128">If retrieving the most up-to-date item is a costly operation (performance-wise,) you can test the Context.PassThru property to determine if you actually need to write the resulting item.</span></span>

## <a name="choose-a-base-class-for-your-provider"></a><span data-ttu-id="b50d9-129">Escolha uma classe Base para o seu provedor</span><span class="sxs-lookup"><span data-stu-id="b50d9-129">Choose a Base Class for Your Provider</span></span>

<span data-ttu-id="b50d9-130">Windows PowerShell fornece um número de classes base que você pode usar para implementar seu próprio provedor do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-130">Windows PowerShell provides a number of base classes that you can use to implement your own Windows PowerShell provider.</span></span> <span data-ttu-id="b50d9-131">Ao criar um provedor, escolha a classe base, descrita nesta seção, que é mais adequada às suas necessidades.</span><span class="sxs-lookup"><span data-stu-id="b50d9-131">When designing a provider, choose the base class, described in this section, that is most suited to your requirements.</span></span>

<span data-ttu-id="b50d9-132">Cada classe de base do provedor do Windows PowerShell disponibiliza um conjunto de cmdlets.</span><span class="sxs-lookup"><span data-stu-id="b50d9-132">Each Windows PowerShell provider base class makes available a set of cmdlets.</span></span> <span data-ttu-id="b50d9-133">Esta seção descreve os cmdlets, mas ele não descreve seus parâmetros.</span><span class="sxs-lookup"><span data-stu-id="b50d9-133">This section describes the cmdlets, but it does not describe their parameters.</span></span>

<span data-ttu-id="b50d9-134">Usando o estado de sessão, o tempo de execução do Windows PowerShell disponibiliza vários cmdlets do local para certos provedores do Windows PowerShell, como o `Get-Location`, `Set-Location`, `Pop-Location`, e `Push-Location` cmdlets.</span><span class="sxs-lookup"><span data-stu-id="b50d9-134">Using the session state, the Windows PowerShell runtime makes several location cmdlets available to certain Windows PowerShell providers, such as the `Get-Location`, `Set-Location`, `Pop-Location`, and `Push-Location` cmdlets.</span></span> <span data-ttu-id="b50d9-135">Você pode usar o `Get-Help` cmdlet para obter informações sobre esses cmdlets de local.</span><span class="sxs-lookup"><span data-stu-id="b50d9-135">You can use the `Get-Help` cmdlet to obtain information about these location cmdlets.</span></span>

### <a name="cmdletprovider-base-class"></a><span data-ttu-id="b50d9-136">Classe base CmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-136">CmdletProvider Base Class</span></span>

<span data-ttu-id="b50d9-137">O [System.Management.Automation.Provider.Cmdletprovider](/dotnet/api/System.Management.Automation.Provider.CmdletProvider) classe define um provedor básico do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-137">The [System.Management.Automation.Provider.Cmdletprovider](/dotnet/api/System.Management.Automation.Provider.CmdletProvider) class defines a basic Windows PowerShell provider.</span></span> <span data-ttu-id="b50d9-138">Esta classe oferece suporte à declaração do provedor e fornece um número de propriedades e métodos que estão disponíveis para todos os provedores do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-138">This class supports the provider declaration and supplies a number of properties and methods that are available to all Windows PowerShell providers.</span></span> <span data-ttu-id="b50d9-139">A classe é invocada com o `Get-PSProvider` cmdlet para listar todos os provedores disponíveis para uma sessão.</span><span class="sxs-lookup"><span data-stu-id="b50d9-139">The class is invoked by the `Get-PSProvider` cmdlet to list all available providers for a session.</span></span> <span data-ttu-id="b50d9-140">A implementação desse cmdlet é fornecida pelo estado de sessão.</span><span class="sxs-lookup"><span data-stu-id="b50d9-140">The implementation of this cmdlet is furnished by the session state.</span></span>

> [!NOTE]
> <span data-ttu-id="b50d9-141">Provedores do Windows PowerShell estão disponíveis para todos os escopos de linguagem do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-141">Windows PowerShell providers are available to all Windows PowerShell language scopes.</span></span>

### <a name="drivecmdletprovider-base-class"></a><span data-ttu-id="b50d9-142">Classe base DriveCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-142">DriveCmdletProvider Base Class</span></span>

<span data-ttu-id="b50d9-143">O [System.Management.Automation.Provider.Drivecmdletprovider](/dotnet/api/System.Management.Automation.Provider.DriveCmdletProvider) classe define um provedor de unidade do Windows PowerShell que dá suporte a operações de adicionando novas unidades, removendo as unidades existentes e inicializando unidades padrão.</span><span class="sxs-lookup"><span data-stu-id="b50d9-143">The [System.Management.Automation.Provider.Drivecmdletprovider](/dotnet/api/System.Management.Automation.Provider.DriveCmdletProvider) class defines a Windows PowerShell drive provider that supports operations for adding new drives, removing existing drives, and initializing default drives.</span></span> <span data-ttu-id="b50d9-144">Por exemplo, o provedor FileSystem fornecido pelo Windows PowerShell inicializa unidades para todos os volumes são montados como discos rígidos e unidades de dispositivo de CD/DVD.</span><span class="sxs-lookup"><span data-stu-id="b50d9-144">For example, the FileSystem provider provided by Windows PowerShell initializes drives for all volumes that are mounted, such as hard drives and CD/DVD device drives.</span></span>

<span data-ttu-id="b50d9-145">Essa classe deriva de [System.Management.Automation.Provider.Cmdletprovider](/dotnet/api/System.Management.Automation.Provider.CmdletProvider) classe base.</span><span class="sxs-lookup"><span data-stu-id="b50d9-145">This class derives from the [System.Management.Automation.Provider.Cmdletprovider](/dotnet/api/System.Management.Automation.Provider.CmdletProvider) base class.</span></span> <span data-ttu-id="b50d9-146">A tabela a seguir lista os cmdlets expostos por esta classe.</span><span class="sxs-lookup"><span data-stu-id="b50d9-146">The following table lists the cmdlets exposed by this class.</span></span> <span data-ttu-id="b50d9-147">Além das listadas, o `Get-PSDrive` cmdlet (exposto pelo estado de sessão) é um cmdlet relacionado que é usado para recuperar as unidades disponíveis.</span><span class="sxs-lookup"><span data-stu-id="b50d9-147">In addition to those listed, the `Get-PSDrive` cmdlet (exposed by session state) is a related cmdlet that is used to retrieve available drives.</span></span>

|<span data-ttu-id="b50d9-148">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-148">Cmdlet</span></span>|<span data-ttu-id="b50d9-149">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-149">Definition</span></span>|
|------------|----------------|
|`New-PSDrive`|<span data-ttu-id="b50d9-150">Cria uma nova unidade para a sessão e transmite as informações da unidade.</span><span class="sxs-lookup"><span data-stu-id="b50d9-150">Creates a new drive for the session, and streams drive information.</span></span>|
|`Remove-PSDrive`|<span data-ttu-id="b50d9-151">Remove uma unidade da sessão.</span><span class="sxs-lookup"><span data-stu-id="b50d9-151">Removes a drive from the session.</span></span>|

### <a name="itemcmdletprovider-base-class"></a><span data-ttu-id="b50d9-152">Classe base ItemCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-152">ItemCmdletProvider Base Class</span></span>

<span data-ttu-id="b50d9-153">O [System.Management.Automation.Provider.Itemcmdletprovider](/dotnet/api/System.Management.Automation.Provider.ItemCmdletProvider) classe define um provedor de item do Windows PowerShell que executa operações em itens individuais do repositório de dados, e não assume nenhum contêiner ou recursos de navegação.</span><span class="sxs-lookup"><span data-stu-id="b50d9-153">The [System.Management.Automation.Provider.Itemcmdletprovider](/dotnet/api/System.Management.Automation.Provider.ItemCmdletProvider) class defines a Windows PowerShell item provider that performs operations on the individual items of the data store, and it does not assume any container or navigation capabilities.</span></span> <span data-ttu-id="b50d9-154">Essa classe deriva de [System.Management.Automation.Provider.Drivecmdletprovider](/dotnet/api/System.Management.Automation.Provider.DriveCmdletProvider) classe base.</span><span class="sxs-lookup"><span data-stu-id="b50d9-154">This class derives from the [System.Management.Automation.Provider.Drivecmdletprovider](/dotnet/api/System.Management.Automation.Provider.DriveCmdletProvider) base class.</span></span> <span data-ttu-id="b50d9-155">A tabela a seguir lista os cmdlets expostos por esta classe.</span><span class="sxs-lookup"><span data-stu-id="b50d9-155">The following table lists the cmdlets exposed by this class.</span></span>

|<span data-ttu-id="b50d9-156">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-156">Cmdlet</span></span>|<span data-ttu-id="b50d9-157">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-157">Definition</span></span>|
|------------|----------------|
|`Clear-Item`|<span data-ttu-id="b50d9-158">Limpa o conteúdo atual de itens no local especificado e substitui-lo com o valor de "Limpar" especificado pelo provedor.</span><span class="sxs-lookup"><span data-stu-id="b50d9-158">Clears the current content of items at the specified location, and replaces it with the "clear" value specified by the provider.</span></span> <span data-ttu-id="b50d9-159">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-159">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Get-Item`|<span data-ttu-id="b50d9-160">Recupera itens do local especificado e, em seguida, transmite os objetos resultantes.</span><span class="sxs-lookup"><span data-stu-id="b50d9-160">Retrieves items from the specified location, and streams the resultant objects.</span></span>|
|`Invoke-Item`|<span data-ttu-id="b50d9-161">Invoca a ação padrão para o item no caminho especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-161">Invokes the default action for the item at the specified path.</span></span>|
|`Set-Item`|<span data-ttu-id="b50d9-162">Define um item no local especificado com o valor indicado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-162">Sets an item at the specified location with the indicated value.</span></span> <span data-ttu-id="b50d9-163">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-163">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Resolve-Path`|<span data-ttu-id="b50d9-164">Resolve os caracteres curinga para um caminho do Windows PowerShell e informações de caminho de fluxos.</span><span class="sxs-lookup"><span data-stu-id="b50d9-164">Resolves the wildcards for a Windows PowerShell path, and streams path information.</span></span>|
|`Test-Path`|<span data-ttu-id="b50d9-165">Testa o caminho especificado e retorna `true` caso ele exista e `false` caso contrário.</span><span class="sxs-lookup"><span data-stu-id="b50d9-165">Tests for the specified path, and returns `true` if it exists and `false` otherwise.</span></span> <span data-ttu-id="b50d9-166">Esse cmdlet é implementado para dar suporte a `IsContainer` parâmetro para o [System.Management.Automation.Provider.Cmdletprovider.Writeitemobject\*](/dotnet/api/System.Management.Automation.Provider.CmdletProvider.WriteItemObject) método.</span><span class="sxs-lookup"><span data-stu-id="b50d9-166">This cmdlet is implemented to support the `IsContainer` parameter for the [System.Management.Automation.Provider.Cmdletprovider.Writeitemobject\*](/dotnet/api/System.Management.Automation.Provider.CmdletProvider.WriteItemObject) method.</span></span>|

### <a name="containercmdletprovider-base-class"></a><span data-ttu-id="b50d9-167">Classe base ContainerCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-167">ContainerCmdletProvider Base Class</span></span>

<span data-ttu-id="b50d9-168">O [System.Management.Automation.Provider.Containercmdletprovider](/dotnet/api/System.Management.Automation.Provider.ContainerCmdletProvider) classe define um provedor de contêiner do Windows PowerShell que expõe um contêiner, para itens de repositório de dados para o usuário.</span><span class="sxs-lookup"><span data-stu-id="b50d9-168">The [System.Management.Automation.Provider.Containercmdletprovider](/dotnet/api/System.Management.Automation.Provider.ContainerCmdletProvider) class defines a Windows PowerShell container provider that exposes a container, for data store items, to the user.</span></span> <span data-ttu-id="b50d9-169">Lembre-se de que um provedor de contêiner do Windows PowerShell pode ser usado somente quando há um contêiner (não há contêineres aninhados) com itens nele.</span><span class="sxs-lookup"><span data-stu-id="b50d9-169">Be aware that a Windows PowerShell container provider can be used only when there is one container (no nested containers) with items in it.</span></span> <span data-ttu-id="b50d9-170">Se houver contêineres aninhados, você deve implementar um provedor de navegação do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-170">If there are nested containers, then you must implement a Windows PowerShell navigation provider .</span></span>

<span data-ttu-id="b50d9-171">Essa classe deriva de [System.Management.Automation.Provider.Itemcmdletprovider](/dotnet/api/System.Management.Automation.Provider.ItemCmdletProvider) classe base.</span><span class="sxs-lookup"><span data-stu-id="b50d9-171">This class derives from the [System.Management.Automation.Provider.Itemcmdletprovider](/dotnet/api/System.Management.Automation.Provider.ItemCmdletProvider) base class.</span></span> <span data-ttu-id="b50d9-172">A tabela a seguir define os cmdlets implementados por esta classe.</span><span class="sxs-lookup"><span data-stu-id="b50d9-172">The following table defines the cmdlets implemented by this class.</span></span>

|<span data-ttu-id="b50d9-173">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-173">Cmdlet</span></span>|<span data-ttu-id="b50d9-174">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-174">Definition</span></span>|
|------------|----------------|
|`Copy-Item`|<span data-ttu-id="b50d9-175">Copia os itens de um local para outro.</span><span class="sxs-lookup"><span data-stu-id="b50d9-175">Copies items from one location to another.</span></span> <span data-ttu-id="b50d9-176">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-176">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Get-Childitem`|<span data-ttu-id="b50d9-177">Recupera os itens filho no local especificado e, em seguida, transmite-os como objetos.</span><span class="sxs-lookup"><span data-stu-id="b50d9-177">Retrieves the child items at the specified location, and streams them as objects.</span></span>|
|`New-Item`|<span data-ttu-id="b50d9-178">Cria novos itens no local especificado e, em seguida, transmite o objeto resultante.</span><span class="sxs-lookup"><span data-stu-id="b50d9-178">Creates new items at the specified location, and streams the resultant object.</span></span>|
|`Remove-Item`|<span data-ttu-id="b50d9-179">Remove itens do local especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-179">Removes items from the specified location.</span></span>|
|`Rename-Item`|<span data-ttu-id="b50d9-180">Renomeia um item no local especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-180">Renames an item at the specified location.</span></span> <span data-ttu-id="b50d9-181">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-181">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|

### <a name="navigationcmdletprovider-base-class"></a><span data-ttu-id="b50d9-182">Classe base NavigationCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-182">NavigationCmdletProvider Base Class</span></span>

<span data-ttu-id="b50d9-183">O [System.Management.Automation.Provider.Navigationcmdletprovider](/dotnet/api/System.Management.Automation.Provider.NavigationCmdletProvider) classe define um provedor de navegação do Windows PowerShell que executa operações para itens que usam mais de um contêiner.</span><span class="sxs-lookup"><span data-stu-id="b50d9-183">The [System.Management.Automation.Provider.Navigationcmdletprovider](/dotnet/api/System.Management.Automation.Provider.NavigationCmdletProvider) class defines a Windows PowerShell navigation provider that performs operations for items that use more than one container.</span></span> <span data-ttu-id="b50d9-184">Essa classe deriva de [System.Management.Automation.Provider.Containercmdletprovider](/dotnet/api/System.Management.Automation.Provider.ContainerCmdletProvider) classe base.</span><span class="sxs-lookup"><span data-stu-id="b50d9-184">This class derives from the [System.Management.Automation.Provider.Containercmdletprovider](/dotnet/api/System.Management.Automation.Provider.ContainerCmdletProvider) base class.</span></span> <span data-ttu-id="b50d9-185">A tabela a seguir lista os cmdlets expostos por esta classe.</span><span class="sxs-lookup"><span data-stu-id="b50d9-185">The following table list the cmdlets exposed by this class.</span></span>

|<span data-ttu-id="b50d9-186">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-186">Cmdlet</span></span>|<span data-ttu-id="b50d9-187">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-187">Definition</span></span>|
|------------|----------------|
|<span data-ttu-id="b50d9-188">Combinar caminhos</span><span class="sxs-lookup"><span data-stu-id="b50d9-188">Combine-Path</span></span>|<span data-ttu-id="b50d9-189">Combina dois caminhos em um único caminho, usando um delimitador específico do provedor entre caminhos.</span><span class="sxs-lookup"><span data-stu-id="b50d9-189">Combines two paths into a single path, using a provider-specific delimiter between paths.</span></span> <span data-ttu-id="b50d9-190">Esse cmdlet transmite as cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="b50d9-190">This cmdlet streams strings.</span></span>|
|`Move-Item`|<span data-ttu-id="b50d9-191">Move os itens no local especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-191">Moves items to the specified location.</span></span> <span data-ttu-id="b50d9-192">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-192">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|

<span data-ttu-id="b50d9-193">Um cmdlet relacionado é o cmdlet do caminho de análise básico fornecido pelo Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="b50d9-193">A related cmdlet is the basic Parse-Path cmdlet furnished by Windows PowerShell.</span></span> <span data-ttu-id="b50d9-194">Esse cmdlet pode ser usado para analisar um caminho do Windows PowerShell para dar suporte a `Parent` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="b50d9-194">This cmdlet can be used to parse a Windows PowerShell path to support the `Parent` parameter.</span></span> <span data-ttu-id="b50d9-195">Ela transmite a cadeia de caracteres de caminho pai.</span><span class="sxs-lookup"><span data-stu-id="b50d9-195">It streams the parent path string.</span></span>

## <a name="select-provider-interfaces-to-support"></a><span data-ttu-id="b50d9-196">Selecione as Interfaces de provedor para suporte</span><span class="sxs-lookup"><span data-stu-id="b50d9-196">Select Provider Interfaces to Support</span></span>

<span data-ttu-id="b50d9-197">Além de derivar de uma das classes base do Windows PowerShell, o provedor do Windows PowerShell pode dar suporte a outras funcionalidades, derivando de uma ou mais das seguintes interfaces de provedor.</span><span class="sxs-lookup"><span data-stu-id="b50d9-197">In addition to deriving from one of the Windows PowerShell base classes, your Windows PowerShell provider can support other functionality by deriving from one or more of the following provider interfaces.</span></span> <span data-ttu-id="b50d9-198">Esta seção define as interfaces e os cmdlets com suporte de cada.</span><span class="sxs-lookup"><span data-stu-id="b50d9-198">This section defines those interfaces and the cmdlets supported by each.</span></span> <span data-ttu-id="b50d9-199">Ele descreve os parâmetros para os cmdlets de interface tem suporte.</span><span class="sxs-lookup"><span data-stu-id="b50d9-199">It does not describe the parameters for the interface-supported cmdlets.</span></span> <span data-ttu-id="b50d9-200">Informações de parâmetro de cmdlet estão disponíveis online usando o `Get-Command` e `Get-Help` cmdlets.</span><span class="sxs-lookup"><span data-stu-id="b50d9-200">Cmdlet parameter information is available online using the `Get-Command` and `Get-Help` cmdlets.</span></span>

### <a name="icontentcmdletprovider"></a><span data-ttu-id="b50d9-201">IContentCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-201">IContentCmdletProvider</span></span>

<span data-ttu-id="b50d9-202">O [System.Management.Automation.Provider.Icontentcmdletprovider](/dotnet/api/System.Management.Automation.Provider.IContentCmdletProvider) interface define um provedor de conteúdo que executa operações no conteúdo de um item de dados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-202">The [System.Management.Automation.Provider.Icontentcmdletprovider](/dotnet/api/System.Management.Automation.Provider.IContentCmdletProvider) interface defines a content provider that performs operations on the content of a data item.</span></span> <span data-ttu-id="b50d9-203">A tabela a seguir lista os cmdlets expostos por esta interface.</span><span class="sxs-lookup"><span data-stu-id="b50d9-203">The following table lists the cmdlets exposed by this interface.</span></span>

|<span data-ttu-id="b50d9-204">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-204">Cmdlet</span></span>|<span data-ttu-id="b50d9-205">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-205">Definition</span></span>|
|------------|----------------|
|`Add-Content`|<span data-ttu-id="b50d9-206">Acrescenta os comprimentos de valor indicado para o conteúdo do item especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-206">Appends the indicated value lengths to the contents of the specified item.</span></span> <span data-ttu-id="b50d9-207">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-207">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Clear-Content`|<span data-ttu-id="b50d9-208">Define o conteúdo do item especificado para o valor "Limpar".</span><span class="sxs-lookup"><span data-stu-id="b50d9-208">Sets the content of the specified item to the "clear" value.</span></span> <span data-ttu-id="b50d9-209">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-209">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Get-Content`|<span data-ttu-id="b50d9-210">Recupera o conteúdo dos itens especificados e transmite os objetos resultantes.</span><span class="sxs-lookup"><span data-stu-id="b50d9-210">Retrieves the contents of the specified items and streams the resultant objects.</span></span>|
|`Set-Content`|<span data-ttu-id="b50d9-211">Substitui o conteúdo existente para os itens especificados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-211">Replaces the existing content for the specified items.</span></span> <span data-ttu-id="b50d9-212">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-212">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|

### <a name="ipropertycmdletprovider"></a><span data-ttu-id="b50d9-213">IPropertyCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-213">IPropertyCmdletProvider</span></span>

<span data-ttu-id="b50d9-214">O [System.Management.Automation.Provider.Ipropertycmdletprovider](/dotnet/api/System.Management.Automation.Provider.IPropertyCmdletProvider) interface define um provedor do Windows PowerShell de propriedade que executa operações nas propriedades de itens no armazenamento de dados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-214">The [System.Management.Automation.Provider.Ipropertycmdletprovider](/dotnet/api/System.Management.Automation.Provider.IPropertyCmdletProvider) interface defines a property Windows PowerShell provider that performs operations on the properties of items in the data store.</span></span> <span data-ttu-id="b50d9-215">A tabela a seguir lista os cmdlets expostos por esta interface.</span><span class="sxs-lookup"><span data-stu-id="b50d9-215">The following table lists the cmdlets exposed by this interface.</span></span>

> [!NOTE]
> <span data-ttu-id="b50d9-216">O `Path` parâmetro sobre esses cmdlets indica um caminho para um item em vez de identificar uma propriedade.</span><span class="sxs-lookup"><span data-stu-id="b50d9-216">The `Path` parameter on these cmdlets indicates a path to an item instead of identifying a property.</span></span>

|<span data-ttu-id="b50d9-217">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-217">Cmdlet</span></span>|<span data-ttu-id="b50d9-218">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-218">Definition</span></span>|
|------------|----------------|
|`Clear-ItemProperty`|<span data-ttu-id="b50d9-219">Define as propriedades dos itens especificados para o valor "Limpar".</span><span class="sxs-lookup"><span data-stu-id="b50d9-219">Sets properties of the specified items to the "clear" value.</span></span> <span data-ttu-id="b50d9-220">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-220">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Get-ItemProperty`|<span data-ttu-id="b50d9-221">Recupera as propriedades dos itens especificados e transmite os objetos resultantes.</span><span class="sxs-lookup"><span data-stu-id="b50d9-221">Retrieves properties from the specified items and streams the resultant objects.</span></span>|
|`Set-ItemProperty`|<span data-ttu-id="b50d9-222">Define as propriedades dos itens especificados com os valores indicados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-222">Sets properties of the specified items with the indicated values.</span></span> <span data-ttu-id="b50d9-223">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-223">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|

### <a name="idynamicpropertycmdletprovider"></a><span data-ttu-id="b50d9-224">IDynamicPropertyCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-224">IDynamicPropertyCmdletProvider</span></span>

<span data-ttu-id="b50d9-225">O [System.Management.Automation.Provider.Idynamicpropertycmdletprovider](/dotnet/api/System.Management.Automation.Provider.IDynamicPropertyCmdletProvider) interface, derivada de [System.Management.Automation.Provider.Ipropertycmdletprovider](/dotnet/api/System.Management.Automation.Provider.IPropertyCmdletProvider), define um provedor que especifica os parâmetros dinâmicos para seus cmdlets com suporte.</span><span class="sxs-lookup"><span data-stu-id="b50d9-225">The [System.Management.Automation.Provider.Idynamicpropertycmdletprovider](/dotnet/api/System.Management.Automation.Provider.IDynamicPropertyCmdletProvider) interface, derived from [System.Management.Automation.Provider.Ipropertycmdletprovider](/dotnet/api/System.Management.Automation.Provider.IPropertyCmdletProvider), defines a provider that specifies dynamic parameters for its supported cmdlets.</span></span> <span data-ttu-id="b50d9-226">Esse tipo de provedor lida com operações para os quais propriedades podem ser definidas em tempo de execução, por exemplo, uma nova operação de propriedade.</span><span class="sxs-lookup"><span data-stu-id="b50d9-226">This type of provider handles operations for which properties can be defined at run time, for example, a new property operation.</span></span> <span data-ttu-id="b50d9-227">Essas operações não são possíveis em itens de ter definido estaticamente propriedades.</span><span class="sxs-lookup"><span data-stu-id="b50d9-227">Such operations are not possible on items having statically defined properties.</span></span> <span data-ttu-id="b50d9-228">A tabela a seguir lista os cmdlets expostos por esta interface.</span><span class="sxs-lookup"><span data-stu-id="b50d9-228">The following table lists the cmdlets exposed by this interface.</span></span>

|<span data-ttu-id="b50d9-229">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-229">Cmdlet</span></span>|<span data-ttu-id="b50d9-230">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-230">Definition</span></span>|
|------------|----------------|
|`Copy-ItemProperty`|<span data-ttu-id="b50d9-231">Copia uma propriedade do item especificado para outro item.</span><span class="sxs-lookup"><span data-stu-id="b50d9-231">Copies a property from the specified item to another item.</span></span> <span data-ttu-id="b50d9-232">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-232">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`Move-ItemProperty`|<span data-ttu-id="b50d9-233">Move uma propriedade do item especificado para outro item.</span><span class="sxs-lookup"><span data-stu-id="b50d9-233">Moves a property from the specified item to another item.</span></span> <span data-ttu-id="b50d9-234">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-234">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|
|`New-ItemProperty`|<span data-ttu-id="b50d9-235">Cria uma propriedade nos itens especificados e transmite os objetos resultantes.</span><span class="sxs-lookup"><span data-stu-id="b50d9-235">Creates a property on the specified items and streams the resultant objects.</span></span>|
|`Remove-ItemProperty`|<span data-ttu-id="b50d9-236">Remove uma propriedade para os itens especificados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-236">Removes a property for the specified items.</span></span>|
|`Rename-ItemProperty`|<span data-ttu-id="b50d9-237">Renomeia uma propriedade dos itens especificados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-237">Renames a property of the specified items.</span></span> <span data-ttu-id="b50d9-238">Esse cmdlet não passa um objeto de saída por meio do pipeline, a menos que seu `PassThru` parâmetro for especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-238">This cmdlet does not pass an output object through the pipeline unless its `PassThru` parameter is specified.</span></span>|

### <a name="isecuritydescriptorcmdletprovider"></a><span data-ttu-id="b50d9-239">ISecurityDescriptorCmdletProvider</span><span class="sxs-lookup"><span data-stu-id="b50d9-239">ISecurityDescriptorCmdletProvider</span></span>

<span data-ttu-id="b50d9-240">O [System.Management.Automation.Provider.Isecuritydescriptorcmdletprovider](/dotnet/api/System.Management.Automation.Provider.ISecurityDescriptorCmdletProvider) interface adiciona a funcionalidade de descritor de segurança para um provedor.</span><span class="sxs-lookup"><span data-stu-id="b50d9-240">The [System.Management.Automation.Provider.Isecuritydescriptorcmdletprovider](/dotnet/api/System.Management.Automation.Provider.ISecurityDescriptorCmdletProvider) interface adds security descriptor functionality to a provider.</span></span> <span data-ttu-id="b50d9-241">Essa interface permite que o usuário obter e definir informações de descritor de segurança para um item no armazenamento de dados.</span><span class="sxs-lookup"><span data-stu-id="b50d9-241">This interface allows the user to get and set security descriptor information for an item in the data store.</span></span> <span data-ttu-id="b50d9-242">A tabela a seguir lista os cmdlets expostos por esta interface.</span><span class="sxs-lookup"><span data-stu-id="b50d9-242">The following table lists the cmdlets exposed by this interface.</span></span>

|<span data-ttu-id="b50d9-243">Cmdlet</span><span class="sxs-lookup"><span data-stu-id="b50d9-243">Cmdlet</span></span>|<span data-ttu-id="b50d9-244">Definição</span><span class="sxs-lookup"><span data-stu-id="b50d9-244">Definition</span></span>|
|------------|----------------|
|`Get-Acl`|<span data-ttu-id="b50d9-245">Recupera as informações contidas em uma lista de controle de acesso (ACL), que é parte de um descritor de segurança usado para proteger recursos do sistema operacional, por exemplo, um arquivo ou um objeto.</span><span class="sxs-lookup"><span data-stu-id="b50d9-245">Retrieves the information contained in an access control list (ACL), which is part of a security descriptor used to guard operating system resources, for example, a file or an object.</span></span>|
|`Set-Acl`|<span data-ttu-id="b50d9-246">Define as informações de uma ACL.</span><span class="sxs-lookup"><span data-stu-id="b50d9-246">Sets the information for an ACL.</span></span> <span data-ttu-id="b50d9-247">Ele está na forma de uma instância do [System.Security.Accesscontrol.Objectsecurity](/dotnet/api/System.Security.AccessControl.ObjectSecurity) no item (NS) designado para o caminho especificado.</span><span class="sxs-lookup"><span data-stu-id="b50d9-247">It is in the form of an instance of [System.Security.Accesscontrol.Objectsecurity](/dotnet/api/System.Security.AccessControl.ObjectSecurity) on the item(s) designated for the specified path.</span></span> <span data-ttu-id="b50d9-248">Esse cmdlet pode definir as informações sobre arquivos, chaves e subchaves no registro, ou qualquer outro item de provedor, se o provedor do Windows PowerShell oferece suporte a configuração de informações de segurança.</span><span class="sxs-lookup"><span data-stu-id="b50d9-248">This cmdlet can set information about files, keys, and subkeys in the registry, or any other provider item, if the Windows PowerShell provider supports the setting of security information.</span></span>|

## <a name="see-also"></a><span data-ttu-id="b50d9-249">Consulte Também</span><span class="sxs-lookup"><span data-stu-id="b50d9-249">See Also</span></span>

[<span data-ttu-id="b50d9-250">Criação de provedores do Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="b50d9-250">Creating Windows PowerShell Providers</span></span>](./how-to-create-a-windows-powershell-provider.md)

[<span data-ttu-id="b50d9-251">Como funciona o Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="b50d9-251">How Windows PowerShell Works</span></span>](http://msdn.microsoft.com/en-us/ced30e23-10af-4700-8933-49873bd84d58)

[<span data-ttu-id="b50d9-252">SDK do Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="b50d9-252">Windows PowerShell SDK</span></span>](../windows-powershell-reference.md)
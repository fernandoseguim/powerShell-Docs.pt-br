---
ms.date: 06/05/2017
keywords: powershell, cmdlet
title: Aprendendo a usar nomes do Windows PowerShell
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 381aa619a41ccacb2ff3a4cdbc2b75b7f04282d1
ms.sourcegitcommit: cf195b090b3223fa4917206dfec7f0b603873cdf
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/09/2018
---
# <a name="learning-windows-powershell-names"></a><span data-ttu-id="a5ad2-103">Aprendendo a usar nomes do Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="a5ad2-103">Learning Windows PowerShell Names</span></span>
<span data-ttu-id="a5ad2-104">Aprender os nomes de comandos e parâmetros de comando é um investimento de tempo significativo com a maioria das interfaces de linha de comando.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-104">Learning names of commands and command parameters is a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="a5ad2-105">O problema é que há pouquíssimos padrões, portanto, a única maneira aprender é memorizar cada comando e cada parâmetro que você precisa usar regularmente.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-105">The issue is that there are very few patterns, so the only way to learn is by memorizing each command and each parameter that you need to use on a regular basis.</span></span>

<span data-ttu-id="a5ad2-106">Quando você trabalha com um novo comando ou o parâmetro, geralmente não é possível usar o que você já sabe; é necessário encontrar e aprender um novo nome.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-106">When you work with a new command or parameter, you cannot generally use what you already know; you have to find and learn a new name.</span></span> <span data-ttu-id="a5ad2-107">Se você observar como as interfaces aumentam de um pequeno conjunto de ferramentas com adições incrementais de funcionalidade, será fácil ver por que a estrutura é diferente do padrão.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-107">If you look at how interfaces grow from a small set of tools with incremental additions to functionality, it is easy to see why the structure is nonstandard.</span></span> <span data-ttu-id="a5ad2-108">Especialmente com nomes de comando, isso pode parecer lógico porque cada comando é uma ferramenta separada, mas há uma maneira melhor de lidar com eles.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-108">With command names in particular, this may sound logical since each command is a separate tool, but there is a better way to handle command names.</span></span>

<span data-ttu-id="a5ad2-109">A maioria dos comandos são criados para gerenciar os elementos do sistema operacional ou aplicativos, como serviços ou processos.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-109">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="a5ad2-110">Os comandos têm uma variedade de nomes que podem ou não caber em uma família.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-110">The commands have a variety of names that may or may not fit into a family.</span></span> <span data-ttu-id="a5ad2-111">Por exemplo, em sistemas Windows, você pode usar os comandos **net start** e **net stop** para iniciar e parar um serviço.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-111">For example, on Windows systems, you can use the **net start** and **net stop** commands to start and stop a service.</span></span> <span data-ttu-id="a5ad2-112">Há outra ferramenta de controle de serviço mais generalizada para Windows que tem um nome completamente diferente, **sc**, que não se ajusta ao padrão de nomenclatura para os comandos de serviço **net**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-112">There is another more generalized service control tool for Windows that has a completely different name, **sc**, that does not fit into the naming pattern for the **net** service commands.</span></span> <span data-ttu-id="a5ad2-113">Para o gerenciamento de processo, o Windows tem o comando **tasklist** para listar processos e **taskkill** para interromper processos.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-113">For process management, Windows has the **tasklist** command to list processes and the **taskkill** command to kill processes.</span></span>

<span data-ttu-id="a5ad2-114">Comandos que usam parâmetros têm especificações de parâmetro irregulares.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-114">Commands that take parameters have irregular parameter specifications.</span></span> <span data-ttu-id="a5ad2-115">Não é possível usar o comando **net start** para iniciar um serviço em um computador remoto.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-115">You cannot use the **net start** command to start a service on a remote computer.</span></span> <span data-ttu-id="a5ad2-116">O comando **sc** iniciará um serviço em um computador remoto, mas para especificá-lo, você deve prefixar o nome com uma barra invertida dupla.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-116">The **sc** command will start a service on a remote computer, but to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="a5ad2-117">Por exemplo, para iniciar o serviço de spooler em um computador remoto denominado DC01, você digitará **sc \\\\DC01 start spooler**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-117">For example, to start the spooler service on a remote computer named DC01, you would type **sc \\\\DC01 start spooler**.</span></span> <span data-ttu-id="a5ad2-118">Para listar as tarefas em execução no DC01, é necessário usar o parâmetro **/S** (para "sistema") e fornecer o nome DC01 sem barras invertidas, desta forma: **tasklist /S DC01**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-118">To list tasks running on DC01, you need to use the **/S** (for "system") parameter and supply the name DC01 without backslashes, like this: **tasklist /S DC01**.</span></span>

<span data-ttu-id="a5ad2-119">Embora existam distinções técnicas importantes entre um serviço e um processo, ambos são exemplos de elementos gerenciáveis em um computador com um ciclo de vida bem definido.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-119">Although there are important technical distinctions between a service and a process, they are both examples of manageable elements on a computer that have a well-defined life cycle.</span></span> <span data-ttu-id="a5ad2-120">Você pode deseja iniciar ou parar um serviço ou processo, ou então obter uma lista de todos os serviços ou processos em execução no momento.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-120">You may want to start or stop a service or process, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="a5ad2-121">Em outras palavras, embora um serviço e um processo sejam coisas diferentes, as ações que executamos em um serviço ou um processo são geralmente conceitualmente as mesmas.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-121">In other words, although a service and a process are different things, the actions we perform on a service or a process are often conceptually the same.</span></span> <span data-ttu-id="a5ad2-122">Além disso, as escolhas que podemos fazer para personalizar uma ação especificando parâmetros de opções também podem ser conceitualmente semelhantes.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-122">Furthermore, choices we may make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="a5ad2-123">O Windows PowerShell explora essas semelhanças para reduzir o número de nomes distintos que você precisa conhecer para compreender e usar os cmdlets.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-123">Windows PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

### <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="a5ad2-124">Cmdlets usam nomes com verbo-substantivo para reduzir a memorização de comandos</span><span class="sxs-lookup"><span data-stu-id="a5ad2-124">Cmdlets Use Verb-Noun Names to Reduce Command Memorization</span></span>
<span data-ttu-id="a5ad2-125">O Windows PowerShell usa um sistema de nomenclatura de "verbo-substantivo", no qual cada nome de cmdlet consiste em um verbo padrão hifenizado com um substantivo específico.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-125">Windows PowerShell uses a "verb-noun" naming system, where each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="a5ad2-126">Os verbos do Windows PowerShell nem sempre são verbos em inglês, mas eles expressam ações específicas no Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-126">Windows PowerShell verbs are not always English verbs, but they express specific actions in Windows PowerShell.</span></span> <span data-ttu-id="a5ad2-127">Os substantivos são muito parecido com os substantivos de qualquer idioma e descrevem tipos específicos de objetos que são importantes na administração do sistema.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-127">Nouns are very much like nouns in any language, they describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="a5ad2-128">É fácil demonstrar como esses nomes em duas partes reduzem o esforço de aprendizagem examinando alguns exemplos de verbos e substantivos.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-128">It is easy to demonstrate how these two-part names reduce learning effort by looking at a few examples of verbs and nouns.</span></span>

<span data-ttu-id="a5ad2-129">Substantivos são menos restritos, mas sempre devem descrever sobre o que um comando atua.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-129">Nouns are less restricted, but they should always describe what a command acts upon.</span></span> <span data-ttu-id="a5ad2-130">O Windows PowerShell tem comandos como **Get-Process**, **Stop-Process**, **Get-Service** e **Stop-Service**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-130">Windows PowerShell has commands such as **Get-Process**, **Stop-Process**, **Get-Service**, and **Stop-Service**.</span></span>

<span data-ttu-id="a5ad2-131">No caso de dois substantivos e dois verbos, a consistência não simplifica muito o aprendizado.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-131">In the case of two nouns and two verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="a5ad2-132">No entanto, se você examinar um conjunto padrão de 10 substantivos e 10 verbos, você terá apenas 20 palavras para entender, mas elas podem ser usadas para formar 100 nomes distintos de comando.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-132">However, if you look at a standard set of 10 verbs and 10 nouns, you then have only 20 words to understand, but those words can be used to form 100 distinct command names.</span></span>

<span data-ttu-id="a5ad2-133">Com frequência, você pode reconhecer o que um comando faz simplesmente lendo seu nome, e é geralmente aparente qual nome deve ser usado para um novo comando.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-133">Frequently, you can recognize what a command does by reading its name, and it is usually apparent what name should be used for a new command.</span></span> <span data-ttu-id="a5ad2-134">Por exemplo, um comando de desligamento do computador poderia ser **Stop-Computer**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-134">For example, a computer shutdown command might be **Stop-Computer**.</span></span> <span data-ttu-id="a5ad2-135">Um comando que lista todos os computadores em uma rede poderia ser **Get-Computer**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-135">A command that lists all computers on a network might be **Get-Computer**.</span></span> <span data-ttu-id="a5ad2-136">O comando que obtém a data do sistema é **Get-Date**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-136">The command that gets the system date is **Get-Date**.</span></span>

<span data-ttu-id="a5ad2-137">Você pode listar todos os comandos que incluem um determinado verbo com o parâmetro **-Verb** para **Get-Command** (abordaremos o **Get-Command** em mais detalhes na próxima seção).</span><span class="sxs-lookup"><span data-stu-id="a5ad2-137">You can list all commands that include a particular verb with the **-Verb** parameter for **Get-Command** (We will discuss **Get-Command** in detail in the next section).</span></span> <span data-ttu-id="a5ad2-138">Por exemplo, para ver todos os cmdlets que usam o verbo **Get**, digite:</span><span class="sxs-lookup"><span data-stu-id="a5ad2-138">For example, to see all cmdlets that use the verb **Get**, type:</span></span>

```
PS> Get-Command -Verb Get
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="a5ad2-139">O parâmetro **-Noun** é ainda mais útil, pois permite ver uma família de comandos que afetam o mesmo tipo de objeto.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-139">The **-Noun** parameter is even more useful because it allows you to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="a5ad2-140">Por exemplo, se você quiser ver quais comandos estão disponíveis para gerenciar serviços, digite o comando a seguir:</span><span class="sxs-lookup"><span data-stu-id="a5ad2-140">For example, if you want to see which commands are available for managing services, type following command:</span></span>

```
PS> Get-Command -Noun Service
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

<span data-ttu-id="a5ad2-141">Um comando não é necessariamente um cmdlet apenas porque ele tem um esquema de nomenclatura verbo-substantivo.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-141">A command is not necessarily a cmdlet, just because it has a verb-noun naming scheme.</span></span> <span data-ttu-id="a5ad2-142">Um exemplo de um comando do Windows PowerShell nativo que não é um cmdlet, mas tem um nome com verbo-substantivo, é o comando para limpar uma janela do console, o Clear-Host.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-142">One example of a native Windows PowerShell command that is not a cmdlet but has a verb-noun name, is the command for clearing a console window, Clear-Host.</span></span> <span data-ttu-id="a5ad2-143">O comando Clear-Host é na verdade uma função interna, como podemos ver ao executar o Get-Command para ele:</span><span class="sxs-lookup"><span data-stu-id="a5ad2-143">The Clear-Host command is actually an internal function, as you can see if you run Get-Command against it:</span></span>

```
PS> Get-Command -Name Clear-Host

CommandType     Name                            Definition
-----------     ----                            ----------
Function        Clear-Host                      $spaceType = [System.Managem...
```

### <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="a5ad2-144">Cmdlets usam parâmetros padrão</span><span class="sxs-lookup"><span data-stu-id="a5ad2-144">Cmdlets Use Standard Parameters</span></span>
<span data-ttu-id="a5ad2-145">Conforme observado anteriormente, comandos usados em interfaces de linha de comando tradicionais geralmente não têm nomes de parâmetro consistentes.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-145">As noted earlier, commands used in traditional command-line interfaces do not generally have consistent parameter names.</span></span> <span data-ttu-id="a5ad2-146">Às vezes, os parâmetros não têm qualquer nome.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-146">Sometimes parameters do not have names at all.</span></span> <span data-ttu-id="a5ad2-147">Quando isso acontece, eles geralmente são caracteres únicos ou palavras abreviadas que podem ser digitadas rapidamente, mas que não são facilmente compreendidas por novos usuários.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-147">When they do, they are often single-character or abbreviated words that can be typed rapidly but are not easily understood by new users.</span></span>

<span data-ttu-id="a5ad2-148">Diferentemente de outras interfaces de linha de comando tradicionais, o Windows PowerShell processa parâmetros diretamente e usa esse acesso direto aos parâmetros com as diretrizes para desenvolvedores a fim de padronizar os nomes dos parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-148">Unlike most other traditional command-line interfaces, Windows PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="a5ad2-149">Embora isso não garante que cada cmdlet sempre esteja em conformidade com os padrões, ele estimula que isso ocorra.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-149">Although this does not guarantee that every cmdlet will always conform to the standards, it does encourage it.</span></span>

> [!NOTE]
> <span data-ttu-id="a5ad2-150">Nomes de parâmetro sempre têm um prefixo '-' acrescentado a eles ao usá-los, de modo a permitir que o Windows PowerShell possa identificá-los claramente como parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-150">Parameter names always have a '-' prepended to them when you use them, to allow Windows PowerShell to clearly identify them as parameters.</span></span> <span data-ttu-id="a5ad2-151">No exemplo **Get-Command -Name Clear-Host**, o nome do parâmetro é **Name**, mas ele é inserido como -**Name**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-151">In the **Get-Command -Name Clear-Host** example, the parameter's name is **Name**, but it is entered as -**Name**.</span></span>

<span data-ttu-id="a5ad2-152">Aqui estão algumas das características gerais dos nomes de parâmetro padrão e seus usos.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-152">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

#### <a name="the-help-parameter-"></a><span data-ttu-id="a5ad2-153">O parâmetro de Ajuda (?)</span><span class="sxs-lookup"><span data-stu-id="a5ad2-153">The Help Parameter (?)</span></span>
<span data-ttu-id="a5ad2-154">Ao especificar o parâmetro **-?**</span><span class="sxs-lookup"><span data-stu-id="a5ad2-154">When you specify the **-?**</span></span> <span data-ttu-id="a5ad2-155">para qualquer cmdlet, este não é executado.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-155">parameter to any cmdlet, the cmdlet is not executed.</span></span> <span data-ttu-id="a5ad2-156">Em vez disso, o Windows PowerShell exibe a ajuda para o cmdlet.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-156">Instead, Windows PowerShell displays help for the cmdlet.</span></span>

#### <a name="common-parameters"></a><span data-ttu-id="a5ad2-157">Parâmetros comuns</span><span class="sxs-lookup"><span data-stu-id="a5ad2-157">Common Parameters</span></span>
<span data-ttu-id="a5ad2-158">O Windows PowerShell tem vários parâmetros, conhecidos como *parâmetros comuns*.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-158">Windows PowerShell has several parameters known as *common parameters*.</span></span> <span data-ttu-id="a5ad2-159">Como esses parâmetros são controlados pelo mecanismo do Windows PowerShell, sempre que eles são implementados por um cmdlet, eles se comportam da mesma maneira.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-159">Because these parameters are controlled by the Windows PowerShell engine, whenever they are implemented by a cmdlet, they will always behave the same way.</span></span> <span data-ttu-id="a5ad2-160">Os parâmetros comuns são **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable** e **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-160">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

#### <a name="suggested-parameters"></a><span data-ttu-id="a5ad2-161">Parâmetros sugeridos</span><span class="sxs-lookup"><span data-stu-id="a5ad2-161">Suggested Parameters</span></span>
<span data-ttu-id="a5ad2-162">Os principais cmdlets do Windows PowerShell usam nomes padrão para parâmetros semelhantes.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-162">The Windows PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="a5ad2-163">Embora o uso de nomes de parâmetros não seja imposto, há diretrizes explícitas para seu uso a fim de incentivar a padronização.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-163">Although the use of parameter names is not enforced, there is explicit guidance for usage to encourage standardization.</span></span>

<span data-ttu-id="a5ad2-164">Por exemplo, as diretrizes recomendam nomear um parâmetro que se refere a um computador pelo nome como **ComputerName**, em vez de Server, Host, System, Node ou outras palavras alternativas comuns.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-164">For example, the guidance recommends naming a parameter that refers to a computer by name as **ComputerName**, rather than Server, Host, System, Node, or other common alternative words.</span></span> <span data-ttu-id="a5ad2-165">Entre os nomes de parâmetro sugeridos importantes estão **Force**, **Exclude**, **Include**, **PassThru**, **Path** e **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="a5ad2-165">Among the important suggested parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>
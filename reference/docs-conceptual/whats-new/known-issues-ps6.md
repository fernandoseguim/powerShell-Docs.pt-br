---
ms.date: 05/17/2018
keywords: powershell,core
title: Problemas conhecidos do PowerShell 6.0
ms.openlocfilehash: ce40a1925e564fbd2c661e70ec36d3842d915dfe
ms.sourcegitcommit: 47becf2823ece251a7264db2387bb503cf3abaa9
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/19/2018
ms.locfileid: "49450989"
---
# <a name="known-issues-for-powershell-60"></a><span data-ttu-id="690d7-103">Problemas conhecidos do PowerShell 6.0</span><span class="sxs-lookup"><span data-stu-id="690d7-103">Known Issues for PowerShell 6.0</span></span>

## <a name="known-issues-for-powershell-on-non-windows-platforms"></a><span data-ttu-id="690d7-104">Problemas conhecidos do PowerShell em plataformas diferentes do Windows</span><span class="sxs-lookup"><span data-stu-id="690d7-104">Known Issues for PowerShell on Non-Windows Platforms</span></span>

<span data-ttu-id="690d7-105">As versões alfa do PowerShell no Linux e no macOS são em grande parte funcionais, mas têm limitações consideráveis e problemas de uso.</span><span class="sxs-lookup"><span data-stu-id="690d7-105">Alpha releases of PowerShell on Linux and macOS are mostly functional but do have some significant limitations and usability issues.</span></span> <span data-ttu-id="690d7-106">As versões beta do PowerShell no Linux e no macOS são mais funcionais e estáveis do que as versões alfa, mas ainda carecem de alguns recursos e podem conter bugs.</span><span class="sxs-lookup"><span data-stu-id="690d7-106">Beta releases of PowerShell on Linux and macOS are more functional and stable than alpha releases, but still may be lacking some set of features, and can contain bugs.</span></span> <span data-ttu-id="690d7-107">Em alguns casos, esses problemas são simplesmente bugs ainda não corrigidos.</span><span class="sxs-lookup"><span data-stu-id="690d7-107">In some cases, these issues are simply bugs that haven't been fixed yet.</span></span> <span data-ttu-id="690d7-108">Em outros casos (como acontece com os aliases padrão para ls, cp etc)., queremos comentários da comunidade sobre as escolhas que fazemos.</span><span class="sxs-lookup"><span data-stu-id="690d7-108">In other cases (as with the default aliases for ls, cp, etc.), we are looking for feedback from the community regarding the choices we make.</span></span>

<span data-ttu-id="690d7-109">Observação: devido a semelhanças dos vários subsistemas subjacentes, o PowerShell no Linux e no macOS tendem a compartilhar o mesmo nível de maturidade com relação a recursos e bugs.</span><span class="sxs-lookup"><span data-stu-id="690d7-109">Note: Due to the similarities of many underlying subsystems, PowerShell on Linux and macOS tend to share the same level of maturity in both features and bugs.</span></span> <span data-ttu-id="690d7-110">Com exceção do que será indicado abaixo, os problemas nesta seção se aplicam aos dois sistemas operacionais.</span><span class="sxs-lookup"><span data-stu-id="690d7-110">Except as noted below, the issues in this section will apply to both operating systems.</span></span>

### <a name="case-sensitivity-in-powershell"></a><span data-ttu-id="690d7-111">Diferenciação de maiúsculas e minúsculas no PowerShell</span><span class="sxs-lookup"><span data-stu-id="690d7-111">Case-sensitivity in PowerShell</span></span>

<span data-ttu-id="690d7-112">Historicamente, o PowerShell nunca diferenciou maiúsculas de minúsculas, com algumas exceções.</span><span class="sxs-lookup"><span data-stu-id="690d7-112">Historically, PowerShell has been uniformly case-insensitive, with few exceptions.</span></span> <span data-ttu-id="690d7-113">Em sistemas operacionais do tipo UNIX, o sistema de arquivos é predominantemente indiferente a maiúsculas e minúsculas, e o PowerShell segue o padrão do sistema de arquivos. Isso é exposto de várias maneiras, óbvias e não óbvias.</span><span class="sxs-lookup"><span data-stu-id="690d7-113">On UNIX-like operating systems, the file system is predominantly case-sensitive and PowerShell adheres to the standard of the file system; this is exposed through a number of ways, obvious and non-obvious.</span></span>

#### <a name="directly"></a><span data-ttu-id="690d7-114">Diretamente</span><span class="sxs-lookup"><span data-stu-id="690d7-114">Directly</span></span>

- <span data-ttu-id="690d7-115">Ao especificar um arquivo no PowerShell, a capitalização correta deve ser usada.</span><span class="sxs-lookup"><span data-stu-id="690d7-115">When specifying a file in PowerShell, the correct case must be used.</span></span>

#### <a name="indirectly"></a><span data-ttu-id="690d7-116">Indiretamente</span><span class="sxs-lookup"><span data-stu-id="690d7-116">Indirectly</span></span>

- <span data-ttu-id="690d7-117">Se um script tentar carregar um módulo, e o nome do módulo não estiver com as maiúsculas e minúsculas corretas, o carregamento do módulo falhará.</span><span class="sxs-lookup"><span data-stu-id="690d7-117">If a script tries to load a module and the module name is not cased correctly, then the module load will fail.</span></span> <span data-ttu-id="690d7-118">Isso pode causar um problema com os scripts existentes, se o nome pelo qual o módulo é chamado não corresponder ao nome real do arquivo.</span><span class="sxs-lookup"><span data-stu-id="690d7-118">This may cause a problem with existing scripts if the name by which the module is referenced doesn't match the actual file name.</span></span>
- <span data-ttu-id="690d7-119">O preenchimento com Tab não ocorrerá automaticamente se a capitalização do nome do arquivo estiver incorreta.</span><span class="sxs-lookup"><span data-stu-id="690d7-119">Tab-completion will not auto-complete if the file name case is wrong.</span></span> <span data-ttu-id="690d7-120">O fragmento para preenchimento deve ter a capitalização correta.</span><span class="sxs-lookup"><span data-stu-id="690d7-120">The fragment to complete must be cased properly.</span></span> <span data-ttu-id="690d7-121">O preenchimento não diferencia maiúsculas de minúsculas para o nome do tipo e conclusões de membro do tipo.</span><span class="sxs-lookup"><span data-stu-id="690d7-121">(Completion is case-insensitive for type name and type member completions.)</span></span>

### <a name="ps1-file-extensions"></a><span data-ttu-id="690d7-122">Extensões de arquivo .PS1</span><span class="sxs-lookup"><span data-stu-id="690d7-122">.PS1 File Extensions</span></span>

<span data-ttu-id="690d7-123">Os scripts do PowerShell devem terminar em `.ps1` para o interpretador entender como carregar e executá-los no processo atual.</span><span class="sxs-lookup"><span data-stu-id="690d7-123">PowerShell scripts must end in `.ps1` for the interpreter to understand how to load and run them in the current process.</span></span> <span data-ttu-id="690d7-124">A execução de scripts no processo atual é o comportamento normal esperado para o PowerShell.</span><span class="sxs-lookup"><span data-stu-id="690d7-124">Running scripts in the current process is the expected usual behavior for PowerShell.</span></span> <span data-ttu-id="690d7-125">O número mágico `#!` pode ser adicionado a um script que não tem uma extensão `.ps1`, mas isso fará com que o script seja executado em uma nova instância do PowerShell, impedindo que o script funcione corretamente ao trocar objetos.</span><span class="sxs-lookup"><span data-stu-id="690d7-125">The `#!` magic number may be added to a script that doesn't have a `.ps1` extension, but this will cause the script to be run in a new PowerShell instance preventing the script from working properly when interchanging objects.</span></span> <span data-ttu-id="690d7-126">Observação: esse pode ser o comportamento desejável ao executar um script do PowerShell do `bash` ou outro shell.</span><span class="sxs-lookup"><span data-stu-id="690d7-126">(Note: this may be the desirable behavior when executing a PowerShell script from `bash` or another shell.)</span></span>

### <a name="missing-command-aliases"></a><span data-ttu-id="690d7-127">Aliases de comando ausentes</span><span class="sxs-lookup"><span data-stu-id="690d7-127">Missing command aliases</span></span>

<span data-ttu-id="690d7-128">No Linux/macOS, os "aliases de conveniência" para os comandos básicos `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` foram removidos.</span><span class="sxs-lookup"><span data-stu-id="690d7-128">On Linux/macOS, the "convenience aliases" for the basic commands `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` have been removed.</span></span> <span data-ttu-id="690d7-129">No Windows, o PowerShell fornece um conjunto de aliases mapeados para nomes de comando do Linux para conveniência do usuário.</span><span class="sxs-lookup"><span data-stu-id="690d7-129">On Windows, PowerShell provides a set of aliases that map to Linux command names for user convenience.</span></span> <span data-ttu-id="690d7-130">Esses aliases foram removidos do PowerShell padrão em distribuições Linux/macOS, permitindo a execução do executável nativo sem especificar um caminho.</span><span class="sxs-lookup"><span data-stu-id="690d7-130">These aliases have been removed from the default PowerShell on Linux/macOS distributions, allowing the native executable to be run without specifying a path.</span></span>

<span data-ttu-id="690d7-131">Há vantagens e desvantagens nisso.</span><span class="sxs-lookup"><span data-stu-id="690d7-131">There are pros and cons to doing this.</span></span> <span data-ttu-id="690d7-132">A remoção dos aliases expõe a experiência do comando nativo ao usuário do PowerShell, mas reduz a funcionalidade no shell, pois os comandos nativos retornam cadeias de caracteres em vez de objetos.</span><span class="sxs-lookup"><span data-stu-id="690d7-132">Removing the aliases exposes the native command experience to the PowerShell user, but reduces functionality in the shell because the native commands return strings instead of objects.</span></span>

> [!NOTE]
> <span data-ttu-id="690d7-133">Essa é uma área sobre a qual a equipe do PowerShell está procurando comentários.</span><span class="sxs-lookup"><span data-stu-id="690d7-133">This is an area where the PowerShell team is looking for feedback.</span></span>
> <span data-ttu-id="690d7-134">Qual é a solução preferida?</span><span class="sxs-lookup"><span data-stu-id="690d7-134">What is the preferred solution?</span></span> <span data-ttu-id="690d7-135">Devemos deixar como está ou adicionar novamente os aliases de conveniência?</span><span class="sxs-lookup"><span data-stu-id="690d7-135">Should we leave it as is or add the convenience aliases back?</span></span> <span data-ttu-id="690d7-136">Consulte a [Edição #929](https://github.com/PowerShell/PowerShell/issues/929).</span><span class="sxs-lookup"><span data-stu-id="690d7-136">See [Issue #929](https://github.com/PowerShell/PowerShell/issues/929).</span></span>

### <a name="missing-wildcard-globbing-support"></a><span data-ttu-id="690d7-137">Falta o suporte ao recurso de curinga</span><span class="sxs-lookup"><span data-stu-id="690d7-137">Missing Wildcard (globbing) Support</span></span>

<span data-ttu-id="690d7-138">No momento, o PowerShell só faz expansão de curinga (recurso de curinga) para cmdlets internos do Windows, e para comandos externos ou binários, bem como os cmdlets no Linux.</span><span class="sxs-lookup"><span data-stu-id="690d7-138">Currently, PowerShell only does wildcard expansion (globbing) for built-in cmdlets on Windows, and for external commands or binaries as well as cmdlets on Linux.</span></span> <span data-ttu-id="690d7-139">Isso significa que um comando como `ls
*.txt` falhará devido à falta de expansão do asterisco para corresponder aos nomes de arquivo.</span><span class="sxs-lookup"><span data-stu-id="690d7-139">This means that a command like `ls
*.txt` will fail because the asterisk will not be expanded to match file names.</span></span> <span data-ttu-id="690d7-140">Você pode contornar esse problema fazendo `ls (gci *.txt | % name)` ou, simplificando, `gci *.txt` usando o equivalente interno do PowerShell para `ls`.</span><span class="sxs-lookup"><span data-stu-id="690d7-140">You can work around this by doing `ls (gci *.txt | % name)` or, more simply, `gci *.txt` using the PowerShell built-in equivalent to `ls`.</span></span>

<span data-ttu-id="690d7-141">Confira [#954](https://github.com/PowerShell/PowerShell/issues/954) para fazer comentários sobre como melhorar a experiência de recurso de curinga no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="690d7-141">See [#954](https://github.com/PowerShell/PowerShell/issues/954) to give us feedback on how to improve the globbing experience on Linux/macOS.</span></span>

### <a name="net-framework-vs-net-core-framework"></a><span data-ttu-id="690d7-142">.NET Framework versus .NET Core Framework</span><span class="sxs-lookup"><span data-stu-id="690d7-142">.NET Framework vs .NET Core Framework</span></span>

<span data-ttu-id="690d7-143">O PowerShell no Linux/macOS usa .NET Core, que é um subconjunto do .NET Framework completo no Microsoft Windows.</span><span class="sxs-lookup"><span data-stu-id="690d7-143">PowerShell on Linux/macOS uses .NET Core which is a subset of the full .NET Framework on Microsoft Windows.</span></span> <span data-ttu-id="690d7-144">Isso é significativo, pois o PowerShell fornece acesso direto aos tipos de estrutura subjacentes, métodos etc. Como resultado, scripts executados no Windows não podem ser executados em outras plataformas devido às diferenças nas estruturas.</span><span class="sxs-lookup"><span data-stu-id="690d7-144">This is significant because PowerShell provides direct access to the underlying framework types, methods, etc. As a result, scripts that run on Windows may not run on non-Windows platforms because of the differences in the frameworks.</span></span> <span data-ttu-id="690d7-145">Para saber mais sobre o .NET Core Framework, confira <https://dotnetfoundation.org/net-core></span><span class="sxs-lookup"><span data-stu-id="690d7-145">For more information about .NET Core Framework, see <https://dotnetfoundation.org/net-core></span></span>

<span data-ttu-id="690d7-146">Com o surgimento do [.NET Standard 2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/), o .NET Core 2.0 trará de volta muitos tipos e métodos tradicionais presentes no .NET Framework completo.</span><span class="sxs-lookup"><span data-stu-id="690d7-146">With the advent of [.NET Standard2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/), .NET Core 2.0 will bring back many of the traditional types and methods present in the full .NET Framework.</span></span> <span data-ttu-id="690d7-147">Isso significa que o PowerShell Core poderá carregar vários módulos tradicionais do Windows PowerShell sem modificação.</span><span class="sxs-lookup"><span data-stu-id="690d7-147">This means that PowerShell Core will be able to load many traditional Windows PowerShell modules without modification.</span></span> <span data-ttu-id="690d7-148">Siga o nosso trabalho relacionado ao .NET Standard 2.0 [aqui](https://github.com/PowerShell/PowerShell/projects/4).</span><span class="sxs-lookup"><span data-stu-id="690d7-148">You can follow our .NET Standard 2.0 related work [here](https://github.com/PowerShell/PowerShell/projects/4).</span></span>

### <a name="redirection-issues"></a><span data-ttu-id="690d7-149">Redirecionamento de problemas</span><span class="sxs-lookup"><span data-stu-id="690d7-149">Redirection Issues</span></span>

<span data-ttu-id="690d7-150">Não há suporte para o redirecionamento de entrada no PowerShell em qualquer plataforma.</span><span class="sxs-lookup"><span data-stu-id="690d7-150">Input redirection is not supported in PowerShell on any platform.</span></span>
[<span data-ttu-id="690d7-151">Edição #1629</span><span class="sxs-lookup"><span data-stu-id="690d7-151">Issue #1629</span></span>](https://github.com/PowerShell/PowerShell/issues/1629)

<span data-ttu-id="690d7-152">Use `Get-Content` para gravar o conteúdo de um arquivo no pipeline.</span><span class="sxs-lookup"><span data-stu-id="690d7-152">Use `Get-Content` to write the contents of a file into the pipeline.</span></span>

<span data-ttu-id="690d7-153">A saída redirecionada conterá a BOM (marca de ordem de byte) Unicode quando a codificação UTF-8 padrão for usada.</span><span class="sxs-lookup"><span data-stu-id="690d7-153">Redirected output will contain the Unicode byte order mark (BOM) when the default UTF-8 encoding is used.</span></span> <span data-ttu-id="690d7-154">A BOM causará problemas ao trabalhar com utilitários que não a esperam, ou ao ser anexada a um arquivo.</span><span class="sxs-lookup"><span data-stu-id="690d7-154">The BOM will cause problems when working with utilities that do not expect it or when appending to a file.</span></span> <span data-ttu-id="690d7-155">Use `-Encoding Ascii` para escrever texto ASCII (o qual, não sendo Unicode, não terá uma BOM).</span><span class="sxs-lookup"><span data-stu-id="690d7-155">Use `-Encoding Ascii` to write ASCII text (which, not being Unicode, will not have a BOM).</span></span>

> [!Note]
> <span data-ttu-id="690d7-156">Confira [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) para nos enviar comentários sobre como melhorar a experiência de codificação no PowerShell Core em todas as plataformas.</span><span class="sxs-lookup"><span data-stu-id="690d7-156">see [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) to give us feedback on improving the encoding experience for PowerShell Core across all platforms.</span></span> <span data-ttu-id="690d7-157">Estamos trabalhando para dar suporte a UTF-8 sem uma BOM e possivelmente alterando os padrões de codificação para diversos cmdlets entre plataformas.</span><span class="sxs-lookup"><span data-stu-id="690d7-157">We are working to support UTF-8 without a BOM and potentially changing the encoding defaults for various cmdlets across platforms.</span></span>

### <a name="job-control"></a><span data-ttu-id="690d7-158">Controle de trabalho</span><span class="sxs-lookup"><span data-stu-id="690d7-158">Job Control</span></span>

<span data-ttu-id="690d7-159">Não há nenhum suporte de controle de trabalho do PowerShell no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="690d7-159">There is no job-control support in PowerShell on Linux/macOS.</span></span>
<span data-ttu-id="690d7-160">Os comandos `fg` e `bg` não estão disponíveis.</span><span class="sxs-lookup"><span data-stu-id="690d7-160">The `fg` and `bg` commands are not available.</span></span>

<span data-ttu-id="690d7-161">Por enquanto, você pode usar os [Trabalhos do PowerShell](/powershell/module/microsoft.powershell.core/about/about_jobs) que funcionam em todas as plataformas.</span><span class="sxs-lookup"><span data-stu-id="690d7-161">For the time being, you can use [PowerShell jobs](/powershell/module/microsoft.powershell.core/about/about_jobs) which do work across all platforms.</span></span>

### <a name="remoting-support"></a><span data-ttu-id="690d7-162">Suporte à comunicação remota</span><span class="sxs-lookup"><span data-stu-id="690d7-162">Remoting Support</span></span>

<span data-ttu-id="690d7-163">Atualmente, o PowerShell Core dá suporte à Comunicação Remota do PowerShell (PSRP) em WSMan com a Autenticação básica no macOS e Linux e com a autenticação baseada em NTLM no Linux.</span><span class="sxs-lookup"><span data-stu-id="690d7-163">Currently, PowerShell Core supports PowerShell Remoting (PSRP) over WSMan with Basic authentication on macOS and Linux, and with NTLM-based authentication on Linux.</span></span> <span data-ttu-id="690d7-164">Não há suporte para a autenticação baseada em Kerberos.</span><span class="sxs-lookup"><span data-stu-id="690d7-164">(Kerberos-based authentication is not supported.)</span></span>

<span data-ttu-id="690d7-165">O trabalho para comunicação remota baseada em WSMan está sendo feito no repositório [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider).</span><span class="sxs-lookup"><span data-stu-id="690d7-165">The work for WSMan-based remoting is being done in the [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider) repo.</span></span>

<span data-ttu-id="690d7-166">O PowerShell Core também oferece suporte à Comunicação Remota do PowerShell (PSRP) sobre SSH em todas as plataformas (Windows, macOS e Linux).</span><span class="sxs-lookup"><span data-stu-id="690d7-166">PowerShell Core also supports PowerShell Remoting (PSRP) over SSH on all platforms (Windows, macOS, and Linux).</span></span> <span data-ttu-id="690d7-167">Embora isso não tenha suporte atualmente em produção, você pode aprender mais sobre como configurar isso [aqui](../core-powershell/ssh-remoting-in-powershell-core.md).</span><span class="sxs-lookup"><span data-stu-id="690d7-167">While this is not currently supported in production, you can learn more about setting this up [here](../core-powershell/ssh-remoting-in-powershell-core.md).</span></span>

### <a name="just-enough-administration-jea-support"></a><span data-ttu-id="690d7-168">Suporte a JEA (Administração Just Enough)</span><span class="sxs-lookup"><span data-stu-id="690d7-168">Just-Enough-Administration (JEA) Support</span></span>

<span data-ttu-id="690d7-169">A capacidade de criar pontos de extremidade de comunicação remota de administração restrita (JEA) não está disponível no PowerShell no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="690d7-169">The ability to create constrained administration (JEA) remoting endpoints is not currently available in PowerShell on Linux/macOS.</span></span> <span data-ttu-id="690d7-170">No momento, esse recurso não está no escopo para 6.0 e é algo que consideraremos após o 6.0, pois requer um trabalho considerável de design.</span><span class="sxs-lookup"><span data-stu-id="690d7-170">This feature is currently not in scope for 6.0 and something we will consider post 6.0 as it requires significant design work.</span></span>

### <a name="sudo-exec-and-powershell"></a><span data-ttu-id="690d7-171">`sudo`, `exec` e PowerShell</span><span class="sxs-lookup"><span data-stu-id="690d7-171">`sudo`, `exec`, and PowerShell</span></span>

<span data-ttu-id="690d7-172">Como o PowerShell executa a maioria dos comandos na memória (como Python ou Ruby), não é possível usar o sudo diretamente com integrações do PowerShell. Você pode, obviamente, executar `pwsh` com o sudo. Se for necessário executar um cmdlet do PowerShell de dentro do PowerShell com sudo, por exemplo, `sudo Set-Date 8/18/2016`, você faria `sudo pwsh Set-Date 8/18/2016`.</span><span class="sxs-lookup"><span data-stu-id="690d7-172">Because PowerShell runs most commands in memory (like Python or Ruby), you can't use sudo directly with PowerShell built-ins. (You can, of course, run `pwsh` from sudo.) If it is necessary to run a PowerShell cmdlet from within PowerShell with sudo, for example, `sudo Set-Date 8/18/2016`, then you would do `sudo pwsh Set-Date 8/18/2016`.</span></span> <span data-ttu-id="690d7-173">Da mesma forma, não é possível executar uma integração do PowerShell diretamente.</span><span class="sxs-lookup"><span data-stu-id="690d7-173">Likewise, you can't exec a PowerShell built-in directly.</span></span> <span data-ttu-id="690d7-174">Em vez disso, será necessário fazer `exec pwsh item_to_exec`.</span><span class="sxs-lookup"><span data-stu-id="690d7-174">Instead you would have to do `exec pwsh item_to_exec`.</span></span>

<span data-ttu-id="690d7-175">Esse problema está sendo acompanhado como parte da [#3232](https://github.com/PowerShell/PowerShell/issues/3232).</span><span class="sxs-lookup"><span data-stu-id="690d7-175">This issue is currently being tracked as part of [#3232](https://github.com/PowerShell/PowerShell/issues/3232).</span></span>

### <a name="missing-cmdlets"></a><span data-ttu-id="690d7-176">Cmdlets ausentes</span><span class="sxs-lookup"><span data-stu-id="690d7-176">Missing Cmdlets</span></span>

<span data-ttu-id="690d7-177">Muitos comandos (cmdlets) normalmente disponíveis no PowerShell não está disponíveis no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="690d7-177">A large number of the commands (cmdlets) normally available in PowerShell are not available on Linux/macOS.</span></span> <span data-ttu-id="690d7-178">Em muitos casos, esses comandos não fazem nenhum sentido nessas plataformas (por exemplo, recursos específicos do Windows como o Registro).</span><span class="sxs-lookup"><span data-stu-id="690d7-178">In many cases, these commands make no sense on these platforms (e.g. Windows-specific features like the registry).</span></span> <span data-ttu-id="690d7-179">Outros comandos, como os comandos de controle de serviço (Get/Start/Stop-Service) estão presentes, mas não são funcionais.</span><span class="sxs-lookup"><span data-stu-id="690d7-179">Other commands like the service control commands (Get/Start/Stop-Service) are present, but not functional.</span></span> <span data-ttu-id="690d7-180">As versões futuras corrigirão esses problemas, arrumando os cmdlets quebrados e adicionando novos valores ao longo do tempo.</span><span class="sxs-lookup"><span data-stu-id="690d7-180">Future releases will correct these problems, fixing the broken cmdlets and adding new ones over time.</span></span>

### <a name="command-availability"></a><span data-ttu-id="690d7-181">Disponibilidade do comando</span><span class="sxs-lookup"><span data-stu-id="690d7-181">Command Availability</span></span>

<span data-ttu-id="690d7-182">A tabela a seguir lista os comandos que não funcionam no PowerShell no Linux/macOS.</span><span class="sxs-lookup"><span data-stu-id="690d7-182">The following table lists commands that are known not to work in PowerShell on Linux/macOS.</span></span>

|<span data-ttu-id="690d7-183">Comandos</span><span class="sxs-lookup"><span data-stu-id="690d7-183">Commands</span></span>|<span data-ttu-id="690d7-184">Estado Operacional</span><span class="sxs-lookup"><span data-stu-id="690d7-184">Operational State</span></span>|<span data-ttu-id="690d7-185">Observações</span><span class="sxs-lookup"><span data-stu-id="690d7-185">Notes</span></span>|
|--------|-----------------|-----|
|<span data-ttu-id="690d7-186">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span><span class="sxs-lookup"><span data-stu-id="690d7-186">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span></span>|<span data-ttu-id="690d7-187">Não disponível.</span><span class="sxs-lookup"><span data-stu-id="690d7-187">Not Available.</span></span>|<span data-ttu-id="690d7-188">Esses comandos não serão reconhecidos.</span><span class="sxs-lookup"><span data-stu-id="690d7-188">These commands will not be recognized.</span></span> <span data-ttu-id="690d7-189">Isso deve ser corrigido em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="690d7-189">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="690d7-190">`Get-Acl`, `Set-Acl`</span><span class="sxs-lookup"><span data-stu-id="690d7-190">`Get-Acl`, `Set-Acl`</span></span>|<span data-ttu-id="690d7-191">Não disponível.</span><span class="sxs-lookup"><span data-stu-id="690d7-191">Not available.</span></span>|<span data-ttu-id="690d7-192">Esses comandos não serão reconhecidos.</span><span class="sxs-lookup"><span data-stu-id="690d7-192">These commands will not be recognized.</span></span> <span data-ttu-id="690d7-193">Isso deve ser corrigido em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="690d7-193">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="690d7-194">`Get-AuthenticodeSignature`, `Set-AuthenticodeSignature`</span><span class="sxs-lookup"><span data-stu-id="690d7-194">`Get-AuthenticodeSignature`, `Set-AuthenticodeSignature`</span></span>|<span data-ttu-id="690d7-195">Não disponível.</span><span class="sxs-lookup"><span data-stu-id="690d7-195">Not available.</span></span>|<span data-ttu-id="690d7-196">Esses comandos não serão reconhecidos.</span><span class="sxs-lookup"><span data-stu-id="690d7-196">These commands will not be recognized.</span></span> <span data-ttu-id="690d7-197">Isso deve ser corrigido em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="690d7-197">This should be fixed in a future release.</span></span>|
|`Wait-Process`|<span data-ttu-id="690d7-198">Disponível, não funciona corretamente.</span><span class="sxs-lookup"><span data-stu-id="690d7-198">Available, doesn't work properly.</span></span> |<span data-ttu-id="690d7-199">Por exemplo, \`Start-Process gvim -PassThru</span><span class="sxs-lookup"><span data-stu-id="690d7-199">For example \`Start-Process gvim -PassThru</span></span> | <span data-ttu-id="690d7-200">Wait-Process\` não funciona; ele falha ao aguardar o processo.</span><span class="sxs-lookup"><span data-stu-id="690d7-200">Wait-Process\` doesn't work; it fails to wait for the process.</span></span>|
|<span data-ttu-id="690d7-201">`Register-PSSessionConfiguration`, `Unregister-PSSessionConfiguration`, `Get-PSSessionConfiguration`</span><span class="sxs-lookup"><span data-stu-id="690d7-201">`Register-PSSessionConfiguration`, `Unregister-PSSessionConfiguration`, `Get-PSSessionConfiguration`</span></span>|<span data-ttu-id="690d7-202">Disponível, mas não funciona.</span><span class="sxs-lookup"><span data-stu-id="690d7-202">Available but doesn't work.</span></span>|<span data-ttu-id="690d7-203">Grava uma mensagem de erro indicando que os comandos não estão funcionando.</span><span class="sxs-lookup"><span data-stu-id="690d7-203">Writes an error message indicating that the commands are not working.</span></span> <span data-ttu-id="690d7-204">Devem ser corrigidos em uma versão futura.</span><span class="sxs-lookup"><span data-stu-id="690d7-204">These should be fixed in a future release.</span></span>|
|<span data-ttu-id="690d7-205">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Register-WmiEvent`, `Remove-Event`, `Unregister-Event`</span><span class="sxs-lookup"><span data-stu-id="690d7-205">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Register-WmiEvent`, `Remove-Event`, `Unregister-Event`</span></span>|<span data-ttu-id="690d7-206">Disponível, mas não há fontes de evento disponíveis.</span><span class="sxs-lookup"><span data-stu-id="690d7-206">Available but no event sources are available.</span></span>|<span data-ttu-id="690d7-207">Os comandos de evento do PowerShell estão presentes, mas a maioria das fontes de evento usadas com os comandos (como System.Timers.Timer) não está disponível no Linux, tornando os comandos inúteis na versão alfa.</span><span class="sxs-lookup"><span data-stu-id="690d7-207">The PowerShell eventing commands are present but most of the event sources used with the commands (such as System.Timers.Timer) are not available on Linux making the commands useless in the Alpha release.</span></span>|
|`Set-ExecutionPolicy`|<span data-ttu-id="690d7-208">Disponível, mas não funciona.</span><span class="sxs-lookup"><span data-stu-id="690d7-208">Available but doesn't work.</span></span>|<span data-ttu-id="690d7-209">Retorna uma mensagem informando que não há suporte nessa plataforma.</span><span class="sxs-lookup"><span data-stu-id="690d7-209">Returns a message saying not supported on this platform.</span></span> <span data-ttu-id="690d7-210">A política de execução é um "dispositivo de segurança" voltado ao usuário que o ajuda a não cometer erros caros.</span><span class="sxs-lookup"><span data-stu-id="690d7-210">Execution policy is a user-focused "safety belt" that helps prevent the user from making expensive mistakes.</span></span> <span data-ttu-id="690d7-211">Não é um limite de segurança.</span><span class="sxs-lookup"><span data-stu-id="690d7-211">It is not a security boundary.</span></span>|
|<span data-ttu-id="690d7-212">`New-PSSessionOption`, `New-PSTransportOption`</span><span class="sxs-lookup"><span data-stu-id="690d7-212">`New-PSSessionOption`, `New-PSTransportOption`</span></span>|<span data-ttu-id="690d7-213">Disponível, mas `New-PSSession` não funciona.</span><span class="sxs-lookup"><span data-stu-id="690d7-213">Available but `New-PSSession` doesn't work.</span></span>|<span data-ttu-id="690d7-214">`New-PSSessionOption` e `New-PSTransportOption` não estão atualmente verificados para funcionar agora que `New-PSSession` funciona.</span><span class="sxs-lookup"><span data-stu-id="690d7-214">`New-PSSessionOption` and `New-PSTransportOption` are not currently verified to work now that `New-PSSession` works.</span></span>|
